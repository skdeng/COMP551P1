import numpy as np

class Model():
	def __init__(self, input_dim)
		self.input_dim = input_dim
		self.output_dim = 1

	def fit(self, x, y):
		self.ty1 = np.sum(y, axis=0) / y.size
		self.tx1 = 

	def forward(self, x):
		if self.ty1 is None:
			raise Exception()

m = Model(4)
